[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "triton_telemetry"
version = "0.1.0"
description = "Triton telemetry package"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]